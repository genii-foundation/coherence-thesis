name: CI

permissions:
  contents: read

on:
  pull_request:
  push:
    branches: [main]

# Cancel superseded runs on the same ref to save minutes.
concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    # This display name is a protected status context. Keep it stable even
    # though the browser job now owns the repository's single build.
    name: Validate (types, lint, unit, build, drift)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: npm

      - name: Bootstrap dependencies
        run: npm run bootstrap

      - name: Materialize manuscript outputs from source
        run: npm run manuscripts:prepare

      - name: Validate repository structure and agent assets
        run: |
          npm --ignore-scripts run repository:validate-layout
          npm --ignore-scripts run repository:validate-agents
          npm --ignore-scripts run repository:source-boundary

      - name: Validate editorial debt
        run: npm --ignore-scripts run editorial:debt

      - name: Validate editorial evidence
        run: npm --ignore-scripts run editorial:validate

      - name: Validate manuscript references and artifact freshness
        run: npm --ignore-scripts run manuscripts:validate

      - name: Type check
        run: npm --ignore-scripts run typecheck

      - name: Lint
        run: npm --ignore-scripts run lint

      - name: Unit tests with coverage
        run: npm --ignore-scripts run test:coverage

      - name: Check source and durable publishing state remain clean
        run: |
          npm --ignore-scripts run repository:source-boundary
          if ! git diff --exit-code -- editorial publishing; then
            echo "::error::Automatic generation changed editorial or publishing state. Run an explicit reviewed write workflow and commit the intended result."
            exit 1
          fi

      - name: Dependency audit (advisory)
        run: npm audit --omit=dev --audit-level=high
        continue-on-error: true

  e2e:
    name: End-to-end (Playwright)
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: npm

      - name: Bootstrap dependencies
        run: npm run bootstrap

      - name: Materialize manuscript outputs from source
        run: npm run manuscripts:prepare

      - name: Build production application
        run: npm --ignore-scripts run build
        env:
          GITHUB_TOKEN: ${{ github.token }}
          UPDATES_REQUIRED_HEAD_SHA: ${{ github.event_name == 'pull_request' && github.event.pull_request.base.sha || github.sha }}
          UPDATES_REFRESH_DEPLOYMENTS: "false"

      - name: Check Updates fallback cache is current
        if: github.event_name == 'pull_request'
        run: npm --ignore-scripts run updates:verify -- "${{ github.event.pull_request.base.sha }}"

      - name: Verify Updates includes the main push
        if: github.event_name == 'push'
        run: npm --ignore-scripts run updates:verify:generated -- "$GITHUB_SHA"

      - name: Check the build preserved editorial and publishing state
        run: |
          if ! git diff --exit-code -- editorial publishing; then
            echo "::error::The production build modified editorial or publishing state. Build output must remain disposable."
            exit 1
          fi

      - name: Install Playwright browsers
        # Most browser coverage runs in Chromium. The focused cover flow
        # regression project runs in WebKit.
        run: npx playwright install --with-deps chromium webkit

      - name: Run end-to-end tests
        run: npm run test:e2e:built
        env:
          CI: "true"

      - name: Upload Playwright report on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 7
