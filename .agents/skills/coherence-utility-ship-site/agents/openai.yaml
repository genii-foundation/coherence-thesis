interface:
  display_name: "Coherence Utility Ship Site"
  short_description: "Publish and verify the production site"
  default_prompt: "Use $coherence-utility-ship-site to merge, publish, and verify an approved production revision with current manuscript audio."

policy:
  allow_implicit_invocation: false
